$('body').append(
  '<div class="ui basic tiny success modal">' +
    '<div class="ui icon header">' +
      '<i class="green checkmark icon"></i>' +
      '<%= @player.name %> has been signed a new Contract.' +
    '</div>' +
  '</div>');

// Output success message
$('.ui.success.modal')
  .modal({
    onHide: function(){
      window.location.reload();
    }
  })
  .modal('show');