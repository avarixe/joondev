// Output success message
$("#new-form").removeClass("disabled");
$(".ui.success.modal").modal({
  onHidden: function(){
    table.ajax.reload(function(){
      var id = "<%= @formation.id %>";
      viewFormation(id);
    });
  }
}).modal("show");