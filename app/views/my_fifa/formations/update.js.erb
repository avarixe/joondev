// Clear any previous error messages and highlights
$('.message').remove();
$('.error').removeClass('error');

// Output succuess message
$('form').prepend(
  '<div class="ui success tiny message" style="display:block">' +
    '<i class="close icon"></i>' +
    '<div class="header">Formation has been successfully saved.</div>' +
  '</div>'
);